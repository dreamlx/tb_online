<h1><%=_ "New %s", Project.human_name %></h1>

<%= error_messages_for :project %>
<% form_for(:project, :url => projects_path) do |f| %>

  <%= render :partial => 'form',:locals => { :f => f }%>
  
  <p>
    <%= submit_tag _("Create") %>
  </p>
<% end %>
<%= link_to _('Back'), projects_path %>
