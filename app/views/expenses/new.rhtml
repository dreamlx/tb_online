<h1><%=_ "New %s", Expense.human_name %></h1>

<%= error_messages_for :expense %>

<% form_for(:expense, :url => expenses_path) do |f| %>
<%= render :partial => 'form',:locals => { :f => f }%>
  <p>
    <%= submit_tag _("Create") %>
  </p>
<% end %>

<%= link_to 'Back', :action => 'index', :prj_id => params[:prj_id] %>
